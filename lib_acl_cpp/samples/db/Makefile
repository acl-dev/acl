.PHONY = all clean

all:
	@(cd query; make)
	@(cd mysql_query; make)
	@(cd mysql_pool; make)
	@(cd mysql_manager; make)

clean:
	@(cd query; make clean)
	@(cd mysql_query; make clean)
	@(cd mysql_pool; make clean)
	@(cd mysql_manager; make clean)
